---
"@cosyte/fhir": patch
---

A JSON array holding another array lost everything the inner array carried, so a status, a code, or a whole resource written that way disappeared from the model under a warning that did not say anything had been dropped.
