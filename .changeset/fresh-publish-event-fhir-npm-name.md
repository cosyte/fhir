---
"@cosyte/fhir": patch
---

Patch bump to 0.0.2 to generate a fresh, never-attempted npm publish event
(FHIR-NPM-NAME). The 0.0.1 publish is blocked by npm's name-similarity security
filter (E403). At npm support's request, this produces a clean new server-side
request for them to trace. No source change.
